// import createHttpClient, { CreateHttpClientSettings } from "networking/client/http/httpClient.js";
// import * as methods from "./httpMethods.js";
// import { RoutesConstraint } from "networking/routes/types.js";

// // type CreateRoutesHttpClientSettings = {
// //   urlBase: string;

// // };

// const createRoutesHttpClient = <Routes extends RoutesConstraint>(
//   routes: Routes,
//   settings: CreateHttpClientSettings
// ): IHttpClient => {
//   const client = createHttpClient(settings);

//   for (const [name, { path, methods }] of Object.entries(routes)) {
//   }

//   return null
// };

// export default createRoutesHttpClient;